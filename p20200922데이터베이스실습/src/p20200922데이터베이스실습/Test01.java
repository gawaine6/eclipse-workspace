package p20200922데이터베이스실습;

import java.awt.FlowLayout;

import javax.swing.JButton;
import javax.swing.JFrame;

public class Test01 {
	public static void main(String[] args) {
		/*
		JFrame f = new JFrame(); // 컨테이너
		JButton btn = new JButton(); // 버튼 하나 추가
		f.setLayout(new FlowLayout()); // 이거 안 넣으면 화면 전체가 한 버튼이 된다
		btn.setText("hello"); // 버튼에 들어가는 텍스트 설정
		f.add(btn); // 컨테이너에 버튼 추가
		f.setSize(300, 200); // 사이즈
		f.setVisible(true);
		f.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // 종료 버튼 클릭시 완전 종료하도록 설정
		*/
		Myframe m = new Myframe();
	}
}
